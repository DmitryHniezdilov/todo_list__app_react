import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({
    todoList: {
        listStyle: 'none',
        margin: '0 0 0.75rem 0',
        padding: '0',
    },
    listGroupItem: {
        padding: '.25rem 0',
    }
});
