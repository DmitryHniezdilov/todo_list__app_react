import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({
  iconButton: {
    padding: 10,
  },

  divider: {
    height: 28,
    margin: 4,
  },
  formControl: {
    flexDirection: "row",
  }
});